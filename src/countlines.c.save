#include <stdio.h>
/* count lines in input */
int main(int argc, char **argv){
	if (argc == 2){
		int c, nl; nl = 0;
		FILE *fp;
		int n = 0;
   		fp = fopen(argv[1],"r");
		if(fp == NULL) {
      			perror("Error in opening file");
      			return(-1);
	   	}
		do {
      			c = fgetc(fp);
      			if( feof(fp) ) {
         			break ;
      		}
     		 printf("%c ", c);
   		} while(1);
		return 0;
	} else {
		printf("usage: %s", argv[0]);
		return 1;
	}
}
