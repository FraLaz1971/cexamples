#include <stdio.h>
int main()
{
    char jerk[20];
    puts("Name some jerk you know:");
    gets(jerk);
    puts("? Yeah,\tI know that jerk.");
    puts(jerk);
    puts("I really agree that's a jerk, too!");
    return 0;
}
